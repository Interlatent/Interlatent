"""interlatent-teleop: direct laptop <-> Pi teleoperation over gRPC.

Distinct top-level module (`interlatent_teleop`) to avoid the namespace
collision the SDK and engine share with `interlatent`.
"""

__version__ = "0.1.0"
